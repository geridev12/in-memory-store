name: in_memory_store
description: A high-performance, in-memory data store for Dart with LRU eviction, batch operations, and optimized memory management for large datasets.
version: 2.0.0
repository: https://github.com/geridev12/in-memory-store.git
homepage: https://github.com/geridev12/in-memory-store.git
topics: [cache, memory, performance, large-datasets, lru]

platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:

environment:
  sdk: ^3.6.2

dependencies:
  collection: ^1.19.0

dev_dependencies:
  test: ^1.26.2
  very_good_analysis: ^7.0.0
